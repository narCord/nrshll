#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>

void determine_type(const char *cmd){
    if (strcmp(cmd, "cd")){

    }
    else if (strcmp(cmd, "exit")){

    }
    else if (strcmp(cmd, "echo")){

    }
    else if (strcmp(cmd, "pwd")){

    }
    else if (strcmp(cmd, "help")){

    }
    else if (strcmp(cmd, "alias")){

    }
    else{

    }
}
